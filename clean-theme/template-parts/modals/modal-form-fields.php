<?php
/**
 * Modal Form Fields Template
 *
 * Template for rendering form fields inside modal windows.
 * This is a helper template - actual rendering is done by clean_theme_render_modal_form_fields().
 *
 * @package Clean_Theme
 * @since Clean Theme 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
    exit; // Exit if accessed directly.
}

// Example usage in custom templates:
// clean_theme_render_modal_form_fields( $fields_array );

?>
<!-- Modal form fields template part -->
<!-- Use clean_theme_render_modal_form_fields( $fields ) to render form fields -->
